<!--Title Card-->
<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/images/main-image-cropped-removebg.png" width="507" height="520" class="d-block mx-lg-auto img-fluid" loading="lazy">
    </div>
    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Explore and drool over various recipe ideas</h1>
        <p class="lead">
            Explore our huge selection of delicious recipe ideas including; easy desserts, delicious vegan and vegetarian 
            dinner ideas, gorgeous pasta recipes, quick bakes, family-friendly meals and gluten-free recipes.
        </p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/random-recipe" class="btn btn-outline-secondary btn-lg btn-lg px-4 me-md-2">Show Random Recipe</a>
        </div>
    </div>
</div>

<!--Main Section - Categories-->

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
    <% if(categories != ""){ %>
        <% categories.forEach(function(category, index){ %>
            <a href="/categories/<%=category.name%>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="images/<%= category.image %>">
                </div>
                <div class="pt-1"><%= category.name %></div>
            </a>
        <% }) %>
    <%}%>
    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="images/view-all.jpg">
        </div>
        <div class="pt-1">View All</div>
    </a>
</div>

<!--Main Section - Latest Recipes-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(recipes != ""){ %>
            <% recipes.forEach(function(recipe, index){ %>
                <a href="/recipe/<%=recipe._id%>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%=recipe.image%>">
                    </div>
                    <div class="pt-1"><%=recipe.name%></div>
                </a>
            <% }) %>
        <%}%>
    </div>

</section>

<!--Main Section - Indian Recipes-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Indian Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof indian !== "undefined" && indian.length > 0){ %>
            <% indian.forEach(function(indianR, index){ %>
                <a href="/recipe/<%=indianR._id%>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%=indianR.image%>">
                    </div>
                    <div class="pt-1"><%=indianR.name%></div>
                </a>
            <% }) %>
        <%}%>
    </div>

</section>

<!--Main Section - Thai Recipes-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Thai Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof thai !== "undefined" && thai.length > 0){ %>
            <% thai.forEach(function(thaiR, index){ %>
                <a href="/recipe/<%=thaiR._id%>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%=thaiR.image%>">
                    </div>
                    <div class="pt-1"><%=thaiR.name%></div>
                </a>
            <% }) %>
        <%}%>
    </div>

</section>

<!--Main Section - American Recipes-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest American Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof american !== "undefined" && american.length > 0){ %>
            <% american.forEach(function(americanR, index){ %>
                <a href="/recipe/<%=americanR._id%>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%=americanR.image%>">
                    </div>
                    <div class="pt-1"><%=americanR.name%></div>
                </a>
            <% }) %>
        <%}%>
    </div>

</section>

<!--Main Section - Chinese Recipes-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Chinese Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof chinese !== "undefined" && chinese.length > 0){ %>
            <% chinese.forEach(function(chineseR, index){ %>
                <a href="/recipe/<%=chineseR._id%>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%=chineseR.image%>">
                    </div>
                    <div class="pt-1"><%=chineseR.name%></div>
                </a>
            <% }) %>
        <%}%>
    </div>

</section>

<!--Main Section - Submit a Recipe-->

<section class="px-4 py-5 my-5 text-center">
    <img src="/images/publish-recipe.png" class="d-block mx-auto mb-4 img-fluid" height="208" width="566">
    <h1 class="display-5 fw-bold">Publish your own recipe TODAY</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Let the world know and relish your recipe!</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
        </div>

    </div>

</section>